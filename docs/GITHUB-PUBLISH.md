# Publishing to GitHub

The project can be pushed to the `ContextKeeper-Android` repository after the repository has been created.

Suggested remote:

```bash
git remote add origin https://github.com/CybersecurityRahbar/ContextKeeper-Android.git
git branch -M main
git push -u origin main
```

Do not put GitHub personal access tokens in source files.
